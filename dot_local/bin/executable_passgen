#!/usr/bin/env sh

# Use the Bitwarden CLIpassword generation to create a random password in the
# format:
#
# XXXX-XXXX-XXXX-XXXX-XXXX-XXXX
#
# where each X is a letter or digit.

p="$(bw generate -uln --length 24)"
echo "$p" | sed 's/\(.\{4\}\)/\1-/g' | sed 's/\(-\)$//g'
