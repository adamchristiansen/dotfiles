#!/usr/bin/env python3

from dotfiles import notify, run

run(["mpc", "clear"], check=True)
notify(
  app="Now Playing ♫",
  body="Playlist cleared",
  icon="media-tape",
  tag="mpd-now-playing",
)
