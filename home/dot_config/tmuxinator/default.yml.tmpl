name: default
root: {{ .chezmoi.homeDir }}

startup_window: term

windows:
  - term:
      layout: even-horizontal
      panes:
        - clear
        - clear
  - music:
      panes: ncmpcpp
      root: {{ .chezmoi.homeDir }}/Documents/Music
  - dots:
      layout: even-horizontal
      panes:
        - clear
        - clear
      root: {{ .chezmoi.sourceDir }}
  - update:
      layout: even-horizontal
      panes:
        - clear
        - clear
